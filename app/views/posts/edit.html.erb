<!DOCTYPE HTML>
<html>
<head>
    <meta charset="UTF-8">
    <link rel="stylesheet" href="users.css">
</head>
<div class="row">
    <div class="col-md-8 col-md-offset-2">
         <div class="form-user style-1 rounded has-shadow-1">
			<div class="row">
				<div class="col-md-6 col-md-offset-3">
					<h1>クチコミ編集</h1>
                   	<%= form_for [@user, @post] do |f| %>

						<div class="actions row">

							<div class="field">
				                <%= f.label :コメント %><br />
				            	<%= f.text_field :comment %>
                            </div>

                        </div>

                             <div class="actions">
				                 <%= f.submit '更新' %>
				             </div>

				            <div>
                               <%= link_to "削除", user_post_path(current_user.id), method: :delete %>
                            </div>
				    <% end %>
			    </div>
		    </div>
         </div>
     </div>
</div>

