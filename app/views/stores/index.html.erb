<div class="main-visual">
    <div class="main-visual-content">
      <!-- pタグ（と文字）を追加 -->
      <p class="main-visual-text">
        The world is a book
        and those who do not travel
        read only one page.
      </p>
      <p class="main-visual-name">
        St. Augustine
      </p>
    </div>
 </div>

 <nav class="nav-list">
 	<div class="col-lg-4" >
        <% if user_signed_in? %>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to " logout", destroy_user_session_path, method: :delete, class: "glyphicon glyphicon-log-out logout" %></li>
          </ul>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
            <li><%= link_to " signup", new_user_registration_path, class: "glyphicon glyphicon-edit sign_up" %></li>
            <li><%= link_to " login", new_user_session_path, class: "glyphicon glyphicon-log-in sign_in" %></li>
          </ul>
        <% end %>
    </div>
  </nav>


<div class="container">
	<h1>お店</h1>
	<div class="row">
		  <% @stores.each do |store| %>

	     <div class="col-lg-4" >
		     <div class="col-lg-12" >
					店名:<%= link_to store.storename, store_path(store.id) %> (<%= link_to store.city %> /<%= link_to store.category %> )<br>
			</div>
		     <div class="col-lg-12" >
		        	<%= attachment_image_tag store,  :image, :fill, 300,300, format: 'jpeg' %>
	     	</div>
	     </div>
	  <% end %>
	</div>
  </div>